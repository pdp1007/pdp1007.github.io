
follow the prompts for the data in the file prompt-for-charts.md and produce the outcome in a separate file, like HTML.


in the HTML file, update the following text "For months we chased signal capture through the browser extension — thousands of tenant admins tried to deploy, yet weekly active users kept sliding and WW had no UUs last month. With the native Edge integration, both tenants and users have exploded, lifting the foundation that powers Copilot Chat, Search & WorkIQ personalization.".

use business-centric language. for example, don't use sliding. instead, say something like modest. instead of exploded, say something like significant increase etc. leverage these as hints and update the text.

in the HTML, don't say "WW recorded no UUs last month". replace it with something like modest etc.

in the HTML, replace "strengthening the foundation that powers Copilot Chat, Search & WorkIQ personalization" with something like "strengthening the foundation that improves personalization across Copilot Chat, Search & WorkIQ"

